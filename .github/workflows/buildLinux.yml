name: CI-Linux

on:
  push:
    branches:
      - master

jobs:
  build-n-run-tests:

    runs-on: self-hosted
    
    steps:
    - name: Pull, Build, and Run Tests
      run: |
        work dir- $(pwd)
        echo user- ${{ inputs.username }}
        echo user- $(whoami)
        git clone https://github.com/simo-andreev/Emotion
