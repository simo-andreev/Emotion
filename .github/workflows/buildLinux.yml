name: CI-Linux

on:
  push:
    branches:
      - master

jobs:
  build-n-run-tests:

    runs-on: self-hosted
    
    steps:
    - name: Pull, Build, and Run Tests
    - run: work dir- $(pwd)
    - run: echo user- ${{ inputs.username }}
    - run: echo user- $(whoami)
    - run: git clone https://github.com/simo-andreev/Emotion
